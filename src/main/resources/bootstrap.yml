spring:
  application:
    name: ConfigurationService
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VadOs1/configuration-repository
        encrypt:
          enabled: false